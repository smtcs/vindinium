<h1>Editing id: {{bot.id}} <a href="/bots/dashboard/{{bot.id}}">Back</a></h1>

{{> editor value=bot.code}}

<br />
<form class="form-button" method="post" id="saveForm">
  <input class="code" type="hidden" name="code"></input>
  <input type="submit" value="Save" class="btn btn-primary"></input>
</form>

{{> run}}

<script>
  $('#saveForm').on('submit', function(ev) {
    ev.preventDefault();
    $('.code').val(editor.getValue());
    this.submit();
  });

  $('#runForm').on('submit', function(ev) {
    ev.preventDefault();
    $('.code').val(editor.getValue());
    this.submit();
  });
</script>
