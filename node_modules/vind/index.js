var request = require('request');

function Bot(key, mode) {
  this.key = key;
  this.mode = mode;
}

Bot.prototype.move = function(dir) {
  console.log('Move', dir);
};

Bot.prototype.key = function() {
  return this.key;
};

module.exports = Bot;
